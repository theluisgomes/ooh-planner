# Environment Variables for OOH Planner

# BigQuery Configuration
GOOGLE_APPLICATION_CREDENTIALS=./config/bigquery-key.json
BIGQUERY_PROJECT_ID=boticario-485202
BIGQUERY_DATASET=Boticario
BIGQUERY_TABLE=inventory

# Server Configuration
# Render injects PORT automatically (default 10000). Do not hardcode it in the dashboard.
PORT=10000
